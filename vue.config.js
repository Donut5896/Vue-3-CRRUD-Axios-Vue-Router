module.exports = {
    devServer: {
        port: 8081
    }
}

//Most HTTP  Server use CORS configuration that accept resources sharing restricted to some site or port,
//so we also need to configure port to our App